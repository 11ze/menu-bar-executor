# 图形界面命令管理功能实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 添加一个图形界面窗口来管理菜单栏命令，支持查看、添加、编辑和删除命令，并自动保存到配置文件。

**架构:** 使用 SwiftUI 创建设置窗口，通过 `CommandsManager` 的 `@Published` 属性实现响应式更新，添加 `ConfigLoader.saveConfig()` 方法持久化命令更改。

**技术栈:**
- SwiftUI 窗口和视图
- Combine 响应式数据绑定
- JSON 编码持久化
- NSWindow 窗口管理

---

## Task 1: 为 ConfigLoader 添加保存方法

**文件:**
- 修改: `Sources/App/ConfigLoader.swift`

**步骤 1: 编写测试验证方法存在（概念测试）**

运行: `swift -c "import Foundation; print('Testing save method design')"`
预期: 此步骤为设计验证，无需实际运行

**步骤 2: 实现 saveConfig 方法**

在 `ConfigLoader` 类中添加以下方法（插入到第 83 行 `}` 之前）:

```swift
func saveConfig(_ commands: [Command]) throws {
    print("[ConfigLoader] 开始保存配置")
    ensureConfigDirectoryExists()

    let config = CommandsConfig(commands: commands)
    let encoder = JSONEncoder()
    encoder.outputFormatting = .prettyPrinted
    let data = try encoder.encode(config)

    try data.write(to: configFilePath)
    print("[ConfigLoader] 配置已保存到: \(configFilePath.path)")
}
```

**步骤 3: 验证代码编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 4: 提交**

```bash
git add Sources/App/ConfigLoader.swift
git commit -m "feat: 添加 ConfigLoader.saveConfig 方法支持保存命令配置"
```

---

## Task 2: 扩展 CommandsManager 添加 CRUD 操作

**文件:**
- 修改: `Sources/App/CommandsManager.swift`

**步骤 1: 添加 addCommand 方法**

在 `CommandsManager` 类中添加（插入到第 45 行 `}` 之前）:

```swift
func addCommand(_ command: Command) {
    commands.append(command)
    do {
        try configLoader.saveConfig(commands)
        print("[CommandsManager] 命令已添加: \(command.name)")
    } catch {
        print("[CommandsManager] 保存失败: \(error)")
    }
}
```

**步骤 2: 添加 updateCommand 方法**

在刚才添加的 `addCommand` 方法后添加:

```swift
func updateCommand(_ command: Command) {
    if let index = commands.firstIndex(where: { $0.id == command.id }) {
        commands[index] = command
        do {
            try configLoader.saveConfig(commands)
            print("[CommandsManager] 命令已更新: \(command.name)")
        } catch {
            print("[CommandsManager] 保存失败: \(error)")
        }
    }
}
```

**步骤 3: 添加 deleteCommand 方法**

继续添加:

```swift
func deleteCommand(id: String) {
    if let index = commands.firstIndex(where: { $0.id == id }) {
        let command = commands[index]
        commands.remove(at: index)
        do {
            try configLoader.saveConfig(commands)
            print("[CommandsManager] 命令已删除: \(command.name)")
        } catch {
            print("[CommandsManager] 保存失败: \(error)")
        }
    }
}
```

**步骤 4: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 5: 提交**

```bash
git add Sources/App/CommandsManager.swift
git commit -m "feat: 添加 CommandsManager 的增删改方法"
```

---

## Task 3: 创建命令编辑器视图

**文件:**
- 创建: `Sources/App/Views/CommandEditorView.swift`

**步骤 1: 创建编辑器视图文件**

创建新文件并写入以下内容:

```swift
import SwiftUI

struct CommandEditorView: View {
    @Environment(\.dismiss) private var dismiss
    let command: Command?
    @State private var name: String
    @State private var commandText: String
    @State private var workingDirectory: String
    @State private var iconName: String
    @State private var notification: Bool
    let onSave: (Command) -> Void

    init(command: Command?, onSave: @escaping (Command) -> Void) {
        self.command = command
        self.onSave = onSave

        if let command = command {
            _name = State(initialValue: command.name)
            _commandText = State(initialValue: command.command)
            _workingDirectory = State(initialValue: command.workingDirectory ?? "")
            _iconName = State(initialValue: command.icon ?? "terminal.fill")
            _notification = State(initialValue: command.notification)
        } else {
            _name = State(initialValue: "")
            _commandText = State(initialValue: "")
            _workingDirectory = State(initialValue: "")
            _iconName = State(initialValue: "terminal.fill")
            _notification = State(initialValue: true)
        }
    }

    var body: some View {
        Form {
            Section("基本信息") {
                TextField("命令名称", text: $name)
                TextField("命令内容", text: $commandText)
                TextField("工作目录（可选）", text: $workingDirectory)
            }

            Section("显示设置") {
                TextField("图标名称（SF Symbol）", text: $iconName)
                Toggle("显示通知", isOn: $notification)
            }

            Section {
                HStack {
                    Button("取消") {
                        dismiss()
                    }
                    .keyboardShortcut(.cancelAction)

                    Spacer()

                    Button("保存") {
                        saveCommand()
                    }
                    .keyboardShortcut(.defaultAction)
                    .disabled(name.isEmpty || commandText.isEmpty)
                }
            }
        }
        .formStyle(.grouped)
        .frame(minWidth: 400, minHeight: 300)
        .padding()
    }

    private func saveCommand() {
        let id = command?.id ?? UUID().uuidString
        let newCommand = Command(
            id: id,
            name: name,
            command: commandText,
            workingDirectory: workingDirectory.isEmpty ? nil : workingDirectory,
            icon: iconName.isEmpty ? nil : iconName,
            notification: notification
        )
        onSave(newCommand)
        dismiss()
    }
}

#Preview {
    CommandEditorView(command: nil) { _ in }
}
```

**步骤 2: 更新 project.yml 添加新文件**

修改 `project.yml` 的 `sources` 部分:

```yaml
sources:
  - Sources
  - Sources/App/Views
```

**步骤 3: 创建 Views 目录并生成项目**

运行: `mkdir -p Sources/App/Views && xcodegen generate`
预期: 项目重新生成成功

**步骤 4: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 5: 提交**

```bash
git add Sources/App/Views/CommandEditorView.swift project.yml
git commit -m "feat: 创建 CommandEditorView 用于添加和编辑命令"
```

---

## Task 4: 创建命令列表管理视图

**文件:**
- 创建: `Sources/App/Views/CommandsListView.swift`

**步骤 1: 创建列表视图文件**

创建新文件并写入:

```swift
import SwiftUI

struct CommandsListView: View {
    @StateObject private var manager = CommandsManager.shared
    @State private var editingCommand: Command?
    @State private var showingEditor = false

    var body: some View {
        VStack(spacing: 0) {
            // 工具栏
            HStack {
                Text("命令管理")
                    .font(.headline)
                Spacer()
                Button(action: {
                    editingCommand = nil
                    showingEditor = true
                }) {
                    Label("添加命令", systemImage: "plus")
                }
            }
            .padding()
            .background(Color(nsColor: .controlBackgroundColor))

            Divider()

            // 命令列表
            List {
                ForEach(manager.commands) { command in
                    HStack {
                        Image(systemName: command.icon ?? "terminal.fill")
                        VStack(alignment: .leading, spacing: 4) {
                            Text(command.name)
                                .font(.body)
                            Text(command.command)
                                .font(.caption)
                                .foregroundColor(.secondary)
                        }
                        Spacer()
                        HStack(spacing: 8) {
                            Button(action: {
                                editingCommand = command
                                showingEditor = true
                            }) {
                                Image(systemName: "pencil")
                            }
                            .buttonStyle(.borderless)

                            Button(action: {
                                deleteCommand(command)
                            }) {
                                Image(systemName: "trash")
                            }
                            .buttonStyle(.borderless)
                        }
                    }
                    .padding(.vertical, 4)
                }
            }
        }
        .frame(minWidth: 600, minHeight: 400)
        .sheet(isPresented: $showingEditor) {
            CommandEditorView(command: editingCommand) { command in
                if editingCommand != nil {
                    manager.updateCommand(command)
                } else {
                    manager.addCommand(command)
                }
            }
        }
    }

    private func deleteCommand(_ command: Command) {
        let alert = NSAlert()
        alert.messageText = "确认删除"
        alert.informativeText = "确定要删除命令「\(command.name)」吗？"
        alert.alertStyle = .warning
        alert.addButton(withTitle: "删除")
        alert.addButton(withTitle: "取消")

        let response = alert.runModal()
        if response == .alertFirstButtonReturn {
            manager.deleteCommand(id: command.id)
        }
    }
}

#Preview {
    CommandsListView()
}
```

**步骤 2: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 3: 提交**

```bash
git add Sources/App/Views/CommandsListView.swift
git commit -m "feat: 创建 CommandsListView 显示和管理命令列表"
```

---

## Task 5: 创建设置窗口管理器

**文件:**
- 创建: `Sources/App/SettingsWindowController.swift`

**步骤 1: 创建窗口控制器文件**

创建新文件并写入:

```swift
import AppKit
import SwiftUI

@MainActor
final class SettingsWindowController: NSWindowController {
    static let shared = SettingsWindowController()

    private init() {
        let contentView = CommandsListView()
        let window = NSWindow(
            contentRect: NSRect(x: 0, y: 0, width: 600, height: 400),
            styleMask: [.titled, .closable, .miniaturizable],
            backing: .buffered,
            defer: false
        )
        window.center()
        window.title = "命令设置"
        window.contentView = NSHostingView(rootView: contentView)
        window.isReleasedWhenClosed = false

        super.init(window: window)
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    func showWindow() {
        window?.makeKeyAndOrderFront(nil)
        NSApp.activate(ignoringOtherApps: true)
    }
}
```

**步骤 2: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 3: 提交**

```bash
git add Sources/App/SettingsWindowController.swift
git commit -m "feat: 创建 SettingsWindowController 管理设置窗口"
```

---

## Task 6: 在菜单中添加设置入口

**文件:**
- 修改: `Sources/App/AppDelegate.swift`

**步骤 1: 在 buildMenu 方法中添加设置菜单项**

找到第 64-73 行（重载配置菜单项部分），在 `menu.addItem(reloadItem)` 之后添加:

```swift
        let settingsItem = NSMenuItem(
            title: "命令设置...",
            action: #selector(openSettings),
            keyEquivalent: ","
        )
        settingsItem.target = self
        menu.addItem(settingsItem)

        menu.addItem(NSMenuItem.separator())
```

**步骤 2: 添加 openSettings 方法**

在第 113 行 `quitApp` 方法之后添加:

```swift
    @objc private func openSettings() {
        SettingsWindowController.shared.showWindow()
    }
```

**步骤 3: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 4: 提交**

```bash
git add Sources/App/AppDelegate.swift
git commit -m "feat: 在菜单栏添加「命令设置」入口"
```

---

## Task 7: 添加图标名称预览功能

**文件:**
- 修改: `Sources/App/Views/CommandEditorView.swift`

**步骤 1: 添加图标预览**

在 `Section("显示设置")` 中，将 `TextField("图标名称（SF Symbol）", text: $iconName)` 替换为:

```swift
                HStack {
                    TextField("图标名称（SF Symbol）", text: $iconName)
                    Image(systemName: iconName.isEmpty ? "terminal.fill" : iconName)
                        .foregroundColor(.secondary)
                }
```

**步骤 2: 验证编译**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 3: 提交**

```bash
git add Sources/App/Views/CommandEditorView.swift
git commit -m "feat: 在编辑器中添加图标预览"
```

---

## Task 8: 添加测试数据和手动测试

**步骤 1: 构建应用**

运行: `xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build`
预期: BUILD SUCCEEDED

**步骤 2: 运行应用进行手动测试**

运行: `open build/Build/Products/Debug/menu-bar-executor.app`

**测试清单:**
- [ ] 点击菜单栏图标，菜单正确显示
- [ ] 点击「命令设置...」打开设置窗口
- [ ] 设置窗口显示现有命令列表
- [ ] 点击「添加命令」打开编辑器
- [ ] 填写命令信息并保存，命令出现在列表中
- [ ] 编辑现有命令并保存，命令正确更新
- [ ] 删除命令，确认对话框正确显示
- [ ] 修改后命令在菜单栏立即生效
- [ ] 配置文件正确保存（检查 `~/.config/menu-bar-executor/commands.json`）
- [ ] 重启应用后配置正确加载

**步骤 3: 提交完成标记**

```bash
git commit --allow-empty -m "test: 完成图形界面命令管理功能的手动测试"
```

---

## 验证命令

构建应用:
```bash
xcodebuild -project menu-bar-executor.xcodeproj -scheme menu-bar-executor -configuration Debug build
```

运行应用:
```bash
open build/Build/Products/Debug/menu-bar-executor.app
```

检查配置文件:
```bash
cat ~/.config/menu-bar-executor/commands.json
```
